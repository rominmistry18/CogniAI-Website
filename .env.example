# ============================================
# Application URLs
# ============================================
# Website URL (public-facing site) - used for development/server-side
WEBSITE_URL=http://localhost:3100

# Admin URL (admin panel)
ADMIN_URL=http://localhost:3007

# Public Website URL for SEO/metadata (use this for production URLs)
# This is used for canonical URLs, schema.org markup, and OpenGraph tags
# In production, set this to your actual domain
# NEXT_PUBLIC_WEBSITE_URL=https://cognaium.com

# Domain configuration (for production routing)
# Set ADMIN_DOMAIN in .env for production (e.g., "admin.cognaium.com")
# ADMIN_DOMAIN=admin.cognaium.com

# ============================================
# AWS S3 Configuration (for Media Storage)
# ============================================
# Add these variables to use AWS S3 for media storage
# If not configured, files will be stored locally in uploads/media/

# AWS Access Key ID
AWS_ACCESS_KEY_ID=your-access-key-id

# AWS Secret Access Key
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# AWS Region (e.g., us-east-1, eu-west-1)
AWS_REGION=us-east-1
# Alternative: AWS_S3_REGION=us-east-1

# S3 Bucket Name
AWS_S3_BUCKET_NAME=your-bucket-name
# Alternative: S3_BUCKET=your-bucket-name

# Optional: Custom CDN URL (if using CloudFront or custom domain)
# If not set, will use standard S3 URL: https://bucket-name.s3.region.amazonaws.com/media/filename
AWS_S3_PUBLIC_URL=https://cdn.yourdomain.com

# ============================================
# Email/SMTP Configuration (for Notifications)
# ============================================
# SMTP server settings for sending email notifications
# When configured, the admin panel can send emails for new leads and job applications

# SMTP Server hostname
SMTP_SERVER=email-smtp.us-east-1.amazonaws.com

# SMTP Port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP Authentication
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password

# Sender information
EMAIL_SENDER_NAME=Cognaium AI
EMAIL_SENDER_ADDRESS=notifications@yourdomain.com

# Enable/Disable email notifications globally
EMAIL_ENABLE_NOTIFICATIONS=true

# Email timeouts and retries
EMAIL_TIMEOUT_SECONDS=30
EMAIL_RETRY_ATTEMPTS=3

# ============================================
# Other Configuration
# ============================================
# Add your other environment variables here
